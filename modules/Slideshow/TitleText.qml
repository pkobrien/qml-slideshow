import QtQuick 2.4
import "." as SS

SS.Text {
    id: titleText

    font.pixelSize: Math.round(slide.height * .1)
    horizontalAlignment: Text.AlignHCenter
    verticalAlignment: Text.AlignVCenter
}
